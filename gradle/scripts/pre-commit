#!/bin/sh

echo "Running ktlint check..."
./gradlew ktlintCheck --daemon

STATUS=$?

if [ $STATUS -ne 0 ]; then
    echo "ktlint found style violations. Please fix them before committing."
    exit 1
fi

exit 0
